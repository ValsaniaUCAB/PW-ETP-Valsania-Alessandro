{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build --host",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "vite": "^5.0.8"
  },
  "dependencies": {
    "socket.io-client": "^4.7.4"
  }
}
